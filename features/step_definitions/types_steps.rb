Given /^we are looking for the "([^\"]*)" class$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given /^we're loading a "([^\"]*)" type fixture$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given /^the fixture is the "([^\"]*)" fixture$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^I load the fixture$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the fixture should have a "([^\"]*)" property of type "([^\"]*)"$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Then /^the fixture should have many embedded Tags$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the fixture should have timestamps$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I add a new tag$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I remove a tag$/ do
  pending # express the regexp above with the code you wish you had
end
